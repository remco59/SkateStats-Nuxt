# Workflow

- Never commit directly to `main`. For every feature/change: create a branch, commit as you go, and open a PR when the feature is ready.
- Only merge a PR when the user explicitly tells you to.
- Use descriptive branch names (e.g. `feature/red-design-theme`).
